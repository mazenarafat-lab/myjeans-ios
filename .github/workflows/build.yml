name: Build iOS App
on:
  push:
    branches: [ "903693b" ]
  workflow_dispatch:
jobs:
  build:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: '3.2'

    - name: Install CocoaPods
      run: |
        gem install cocoapods
        pod repo add-cdn trunk https://cdn.cocoapods.org/
        pod install --repo-update

    - name: Ensure Directory and Create GoogleService-Info.plist
      run: |
        mkdir -p MyJeansApp
        cat > MyJeansApp/GoogleService-Info.plist << 'EOF'
        <?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
        <plist version="1.0">
        <dict>
            <key>API_KEY</key>
            <string>${{ secrets.FIREBASE_API_KEY }}</string>
            <key>GCM_SENDER_ID</key>
            <string>${{ secrets.FIREBASE_GCM_SENDER_ID }}</string>
            <key>BUNDLE_ID</key>
            <string>com.myjeans.app</string>
            <key>PROJECT_ID</key>
            <string>myjeans-app-v3</string>
            <key>GOOGLE_APP_ID</key>
            <string>${{ secrets.FIREBASE_GOOGLE_APP_ID }}</string>
        </dict>
        </plist>
        EOF

    - name: Build for Simulator
      run: |
        xcodebuild build \
          -workspace MyJeansApp.xcworkspace \
          -scheme MyJeansApp \
          -sdk iphonesimulator \
          -destination 'platform=iOS Simulator,name=iPhone 16' \
          -configuration Debug \
          -derivedDataPath build_output \
          CODE_SIGNING_ALLOWED=NO \
          CODE_SIGNING_REQUIRED=NO \
          CODE_SIGN_IDENTITY="" \
          | tail -50

    - name: Package App
      run: |
        echo "Searching for App..."
        # Find the actual folder, ignoring any symbolic links first
        REAL_APP_PATH=$(find build_output -name "MyJeansApp.app" -type d | grep "iphonesimulator" | head -1)
        
        if [ -z "$REAL_APP_PATH" ]; then
          echo "ERROR: App bundle not found in build_output!"
          ls -R build_output
          exit 1
        fi
        
        echo "Found App at: $REAL_APP_PATH"
        mkdir -p final_payload
        
        # Copy with -RL to turn shortcuts into real files
        cp -RL "$REAL_APP_PATH" final_payload/
        
        # Zip it up
        cd final_payload && zip -r ../MyJeansApp-Simulator.zip MyJeansApp.app

    - name: Upload
      uses: actions/upload-artifact@v4
      with:
        name: MyJeansApp-Simulator
        path: MyJeansApp-Simulator.zip
        retention-days: 5